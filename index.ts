import { Search as _search } from './src/search'
import { Details as _Details } from './src/Details'
import { recordType as _recordType } from './src/type'

export namespace chemistry {
    export const Search = _search;
    export const Details = _Details;

    export const recordType = _recordType;
}